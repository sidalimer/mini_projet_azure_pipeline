# Base URL of the existing FastAPI service (POST /jobs, GET /jobs/{id})
VITE_API_BASE_URL=https://api-doc-vl.azurewebsites.net

# Base URL of the Azure Functions app (must expose /api/negotiate)
VITE_FUNCTIONS_BASE_URL=https://<your-functions-app>.azurewebsites.net
